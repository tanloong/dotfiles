#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'

pamixer -i 5 --allow-boost

notify-send --urgency low \
    --hint string:x-canonical-private-synchronous:volume \
    "Volume: $(pamixer --get-volume)" -t 2000
# update statusbar
status=$(slstatus -1)
xsetroot -name "$status"
