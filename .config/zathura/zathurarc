# website: https://pwmt.org/projects/zathura/documentation/
#
# One can press f to highlight all links on the page and assign them a number, typing the number of the link and pressing enter will then jump to the link's location. If F is used, only the location of the link will be shown in the status bar.
# Commands may be entered directly into zathura by pressing :, just like in vi.

# define the gap in pixels between rendered pages
    set page-padding 5
# display status bar and vertical scrolling bar
    set guioptions sv
set selection-clipboard clipboard
set sandbox none
set statusbar-basename true
set scroll-step 40
set zoom-step 10
set incremental-search true

map b toggle_statusbar
map r reload
map R rotate
map I recolor
map <C-j> zoom out
map <C-k> zoom in
map - zoom out
map = zoom in
map u scroll half-up
map d scroll half-down
map H scroll full-up
map L scroll full-down
map h scroll left
map l scroll right
map <Left> scroll full-up
map <Right> scroll full-down
map ` mark_evaluate
map D toggle_page_mode
map p print


# set statusbar-h-padding 0
# set statusbar-v-padding 0
