#!/usr/bin/env lua
require('Comment').setup({
    toggler = {
        line = 'gc',
        block = 'gb',
    },
    opleader = {
        line = 'gc',
        block = 'gb',
    },
    mappings = {
        basic = true,
        extra = false,
    },
})
